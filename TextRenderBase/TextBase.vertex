#version 330

layout(location = 0) in vec2 aposition;
layout(location = 1) in vec2 atexcoord;
out vec2 texcoord;
uniform mat4 projection;
void main()
{
	texcoord = atexcoord;
	gl_Position = projection*vec4(aposition.xy,0.f,1.0f);
}